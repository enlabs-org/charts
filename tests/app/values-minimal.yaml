---
# Minimal test values for app chart
# TLS with letsencrypt is enabled by default

components:
  web:
    image: "nginx:latest"
    containerPort: 80
    ingress:
      enabled: true
      host: "minimal.example.com"
